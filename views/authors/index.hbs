		<div class="row">
			<ol class="breadcrumb">
				<li><a href="#">
					<em class="fa fa-users"></em>
				</a></li>
				<li class="active">Authors</li>
			</ol>
		</div><!--/.row-->
        
<div class="card mb-4 mt-5">
    <div class="card-body">
    <div class="table-responsive">

    <div class="col-md-12 text-right">
        <a href="/authors/create" class="btn btn-primary">Add Author</a>
    </div>
    <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Adress</th>
                    <th>Telephone</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{#each authors}}
					{{#dataValues}}
                    <tr>
                        <td>{{ this.name }}</td>
                        <td>{{ this.adress }}</td>
                        <td>{{ this.telephon }}</td>
                        <td>
                            <form action="/authors/delete/{{ this.id }}" method="post">
                                <a href="/authors/edit/{{ this.id }}" class="btn btn-info  rounded-circle"><i class="fa fa-pencil"></i> </a>
                                <button type="submit" class="btn btn-danger  rounded-circle"><i class="fa fa-trash"></i> </button>
                            </form>
                        </td>
                    </tr>
                {{/dataValues}}
                {{/each}}
            </tbody>
        </table>
    </div>
</div>
</div>